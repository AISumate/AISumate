# This project is its own pnpm workspace root (see pnpm-workspace.yaml for why).
# Being a root means `pnpm add` would otherwise warn ERR_PNPM_ADDING_TO_ROOT on
# every install; this is a single-package repo, so adding to the root is exactly
# what we want.
ignore-workspace-root-check=true
